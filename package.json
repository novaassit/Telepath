{
  "name": "telegram-ollama-bot",
  "version": "1.0.0",
  "description": "Telegram chatbot powered by local Ollama LLM",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:electron": "tsc -p tsconfig.electron.json && node scripts/copy-renderer.js && electron dist-electron/main.js",
    "build": "tsc",
    "build:electron": "tsc -p tsconfig.electron.json && node scripts/copy-renderer.js",
    "start": "node dist/index.js",
    "start:electron": "electron dist-electron/main.js",
    "webhook:delete": "tsx scripts/delete-webhook.ts",
    "claude:test": "tsx scripts/test-claude-key.ts"
  },
  "dependencies": {
    "grammy": "^1.35.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "electron": "^35.0.0"
  }
}
